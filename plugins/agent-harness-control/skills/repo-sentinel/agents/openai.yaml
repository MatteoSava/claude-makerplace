interface:
  display_name: "Repo Sentinel"
  short_description: "Deterministic repo guardrails for Claude Code and Codex"
  default_prompt: "Use Repo Sentinel to inspect repository hook policy and verification gates."
policy:
  allow_implicit_invocation: true
